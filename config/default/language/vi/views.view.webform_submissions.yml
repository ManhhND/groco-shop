display:
  default:
    display_options:
      pager:
        options:
          expose:
            items_per_page_label: 'Số mục trên một trang'
      fields:
        view_webform_submission:
          label: 'Thao tác'
      arguments:
        in_draft:
          exception:
            title: 'Tất cả'
  embed_administer:
    display_options:
      fields:
        operations:
          label: 'Thao tác'
  embed_manage:
    display_options:
      fields:
        view_webform_submission:
          label: 'Thao tác'
        edit_webform_submission:
          label: 'Thao tác'
  embed_review:
    display_options:
      fields:
        view_webform_submission:
          label: 'Thao tác'
